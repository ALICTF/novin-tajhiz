# راهنمای استقرار — نوین تجهیز

## استک

| لایه | انتخاب | چرا |
|---|---|---|
| اپ | Next.js 15 (App Router) | همان فرانت‌اند موجود؛ سرور و کلاینت یکجا |
| دیتابیس | PostgreSQL 17 | چند اتصال هم‌زمان، داده ماندگار، پشتیبانی همه هاست‌های ایرانی |
| ORM | Prisma 7 با آداپتور `pg` | مهاجرت نسخه‌دار، تایپ‌سیف |
| فایل‌ها | دیسک محلی یا S3-سازگار | با یک متغیر محیطی عوض می‌شود |
| اجرا | Docker چندمرحله‌ای، خروجی standalone | ایمیج کوچک، بدون ابزار build |

---

## ۱. اجرای محلی

```bash
cp .env.example .env      # مقادیر را پر کنید
docker compose up -d db   # فقط دیتابیس
npx prisma migrate deploy
npx tsx prisma/seed.ts    # کاتالوگ اولیه
npm run dev
```

سایت روی `http://localhost:3000` و پنل روی `http://localhost:3000/admin`.

---

## ۲. استقرار روی سرور اختصاصی (پارس‌پک)

سرور باید Docker و Docker Compose داشته باشد.

```bash
git clone <آدرس مخزن> && cd novin-tajhiz-main
cp .env.example .env
```

در `.env` این‌ها را حتماً عوض کنید:

```dotenv
POSTGRES_PASSWORD=یک-رمز-قوی
ADMIN_PASSWORD=رمز-ورود-پنل
ADMIN_SESSION_SECRET=یک-رشته-تصادفی-حداقل-۳۲-نویسه
```

برای ساخت کلید تصادفی:

```bash
openssl rand -hex 32
```

سپس:

```bash
docker compose up -d --build
```

همین. `entrypoint` خودش منتظر دیتابیس می‌ماند، مهاجرت‌ها را اجرا می‌کند و اگر
کاتالوگ خالی باشد داده اولیه را وارد می‌کند.

بررسی سلامت:

```bash
curl http://localhost:3000/api/health      # {"status":"ok"}  ← زنده بودن سرویس
curl http://localhost:3000/api/health/db   # {"status":"ok","database":"up"}
docker compose logs -f app
```

### پشت nginx

```nginx
server {
    listen 80;
    server_name novintajhiz.org www.novintajhiz.org;

    client_max_body_size 6m;   # آپلود تصویر تا ۵ مگابایت

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

بعد از افزودن SSL (مثلاً با certbot)، کوکی نشست پنل خودکار `Secure` می‌شود
چون `NODE_ENV=production` است.

### پشتیبان‌گیری

سرویس `backup` در `docker-compose.yml` خودکار اجرا می‌شود — نیازی به cron روی
سرور نیست. هر ۲۴ ساعت یک نسخه از دیتابیس و یک نسخه از فایل‌های آپلودی
می‌گیرد و نسخه‌های قدیمی‌تر از ۱۴ روز را پاک می‌کند.

نسخه‌ها در پوشه `./backups` کنار پروژه می‌مانند (نه داخل volume داکر)، پس با
`docker compose down -v` هم پاک نمی‌شوند.

```
backups/
├── db/novintajhiz-2026-09-06_0300.dump
└── uploads/uploads-2026-09-06_0300.tar.gz
```

تنظیم فاصله و مدت نگهداری در `.env`:

```dotenv
BACKUP_INTERVAL_HOURS="24"
BACKUP_RETENTION_DAYS="14"
```

گرفتن نسخه دستی، بدون انتظار برای نوبت بعدی:

```bash
docker compose exec backup /backup.sh
```

بررسی وضعیت سرویس:

```bash
docker compose logs -f backup
```

#### بازیابی

دیتابیس با فرمت custom ذخیره می‌شود، پس `pg_restore` می‌تواند به‌صورت انتخابی
هم بازیابی کند.

```bash
# بازیابی کامل روی یک دیتابیس خالی
docker compose exec -T db psql -U novin -d postgres   -c "DROP DATABASE IF EXISTS novintajhiz;" -c "CREATE DATABASE novintajhiz;"

docker compose exec -T db pg_restore -U novin -d novintajhiz --no-owner   < backups/db/novintajhiz-2026-09-06_0300.dump
```

فایل‌های آپلودی:

```bash
docker run --rm -v novin-tajhiz-main_uploads:/data   -v "$(pwd)/backups/uploads:/backup" alpine   tar xzf /backup/uploads-2026-09-06_0300.tar.gz -C /data
```

> **نسخه پشتیبانی که تستش نکرده باشید، پشتیبان نیست.** ماهی یک‌بار روی یک
> دیتابیس آزمایشی بازیابی کنید و تعداد رکوردها را با سایت زنده مقایسه کنید.

---

## ۳. استقرار روی لیارا

لیارا برای پلتفرم Docker **دیسک ماندگار** می‌دهد، پس نیازی به خرید فضای ابری
جداگانه ندارید. فقط دو چیز لازم است:

۱. **دیتابیس** را از خود لیارا بگیرید (سرویس PostgreSQL) و `DATABASE_URL` را در
   متغیرهای محیطی برنامه بگذارید.

۲. یک **دیسک به نام `uploads`** بسازید. فایل `liara.json` از قبل آن را روی مسیر
   `/app/uploads` سوار می‌کند:

```json
"disks": [{ "name": "uploads", "mountTo": "/app/uploads" }]
```

بدون این دیسک، فایل‌سیستم برنامه بعد از هر استقرار برمی‌گردد به حالت اول و هر
تصویر یا فیشی که آپلود شده پاک می‌شود.

متغیرهای محیطی در پنل لیارا:

```
DATABASE_URL=postgresql://...          # از سرویس دیتابیس لیارا
NEXT_PUBLIC_SITE_URL=https://<app>.liara.run
ADMIN_PASSWORD=...
ADMIN_SESSION_SECRET=...
UPLOAD_DIR=/app/uploads
TZ=Asia/Tehran
```

> `NEXT_PUBLIC_SITE_URL` را تا قبل از اتصال دامنه روی زیردامنه لیارا بگذارید و
> بعد از اتصال دامنه عوضش کنید. فید ترب و sitemap از همین می‌خوانند.

سپس:

```bash
liara deploy
```

### دو بررسی سلامت

| مسیر | چه چیزی را چک می‌کند | مصرف‌کننده |
|---|---|---|
| `/api/health` | فقط زنده بودن پروسه | داکر و لیارا (مبنای ری‌استارت) |
| `/api/health/db` | اتصال دیتابیس | مانیتورینگ و بررسی دستی |

این جدایی عمدی است. اگر بررسی سلامتِ لیارا دیتابیس را چک می‌کرد، با هر قطعی
دیتابیس کانتینر ناسالم علامت می‌خورد و ری‌استارت می‌شد — در حالی که ری‌استارت
کردن اپ دیتابیس را درست نمی‌کند و فقط سایت را کاملاً پایین می‌آورد. با این
تقسیم، صفحه‌های عمومی با کش سرپا می‌مانند تا دیتابیس برگردد.

### چه وقت به فضای ابری (S3) نیاز پیدا می‌کنید؟

دیسک برای یک نمونه از برنامه کافی است. اگر روزی یکی از این‌ها پیش آمد، سراغ
فضای ابری بروید:

- برنامه را روی **بیش از یک نمونه** اجرا کنید (دیسک بین نمونه‌ها مشترک نیست).
- حجم فایل‌ها از اندازه دیسک بیشتر شود.
- بخواهید تصاویر از **CDN** سرو شوند و بار از روی سرور برداشته شود.

در آن صورت فقط این متغیرها را پر کنید؛ هیچ تغییری در کد لازم نیست و آپلودهای
جدید خودکار به فضای ابری می‌روند:

```
S3_ENDPOINT=https://storage.iran.liara.space
S3_BUCKET=novin-uploads
S3_ACCESS_KEY=...
S3_SECRET_KEY=...
S3_PUBLIC_URL=https://novin-uploads.storage.iran.liara.space
```

> فایل‌هایی که قبلاً روی دیسک بوده‌اند خودکار منتقل نمی‌شوند؛ باید یک‌بار
> دستی کپی شوند.

## ۴. متغیرهای محیطی

| متغیر | الزامی | توضیح |
|---|---|---|
| `DATABASE_URL` | بله | رشته اتصال PostgreSQL |
| `NEXT_PUBLIC_SITE_URL` | **بله** | آدرس نهایی سایت. در فید ترب، sitemap و تگ‌های اشتراک‌گذاری به‌کار می‌رود؛ اگر اشتباه باشد ترب محصولات را پیدا نمی‌کند. |
| `ADMIN_PASSWORD` | بله | رمز ورود پنل |
| `ADMIN_SESSION_SECRET` | بله | کلید امضای کوکی، حداقل ۱۶ نویسه |
| `UPLOAD_DIR` | خیر | مسیر آپلود روی دیسک (پیش‌فرض `./uploads`). روی سرور باید به یک volume یا دیسک ماندگار اشاره کند. |
| `S3_ENDPOINT` | خیر | اگر پر باشد آپلودها به فضای ابری می‌روند |
| `S3_BUCKET` | خیر | نام باکت |
| `S3_REGION` | خیر | پیش‌فرض `us-east-1` |
| `S3_ACCESS_KEY` | خیر | کلید دسترسی |
| `S3_SECRET_KEY` | خیر | کلید مخفی |
| `S3_PUBLIC_URL` | خیر | دامنه عمومی باکت |
| `SEED_ON_START` | خیر | `false` بگذارید تا داده اولیه هرگز وارد نشود |
| `KAVENEGAR_API_KEY` | خیر | کلید پنل کاوه‌نگار. تا پر نشود پیامکی ارسال نمی‌شود و سایت عادی کار می‌کند. |
| `KAVENEGAR_SENDER` | خیر | خط فرستنده. خالی = خط پیش‌فرض حساب |
| `ADMIN_SMS_RECIPIENT` | خیر | شماره دریافت پیامک سفارش. خالی = شماره اصلی در `site.ts` |
| `KAVENEGAR_BASE_URL` | خیر | فقط برای تست؛ آدرس سرویس پیامک را عوض می‌کند تا بدون مصرف اعتبار واقعی بتوان ارسال را آزمود. در تولید خالی بگذارید. |
| `NODE_OPTIONS` | خیر | روی پلن ۵۱۲ مگابایتی لیارا `--max-old-space-size=384` بگذارید. بدون آن Node فرض می‌کند کل رم ماشین مال اوست، دیر زباله‌روبی می‌کند و می‌تواند به سقف کانتینر بخورد. |

---

## ۵. پیامک سفارش (کاوه‌نگار)

با ثبت هر سفارش، یک پیامک برای مدیریت می‌رود:

```
سفارش جدید ثبت شد
کد: NT-2609-37227
کوروش شاپوری - 09151234567
۱ قلم - ۴۰,۰۰۰,۰۰۰ تومان
```

برای فعال کردنش فقط کافی است کلید را در متغیرهای محیطی بگذارید:

```dotenv
KAVENEGAR_API_KEY="کلید-پنل-کاوه-نگار"
KAVENEGAR_SENDER=""              # خالی = خط پیش‌فرض حساب
ADMIN_SMS_RECIPIENT="09151234567"  # خالی = شماره اصلی site.ts
```

دو تصمیم که خوب است بدانید:

- **تا وقتی کلید خالی باشد هیچ خطایی رخ نمی‌دهد.** سفارش‌ها عادی ثبت می‌شوند و
  فقط یک خط در لاگ سرور نوشته می‌شود. پس می‌توانید سایت را بدون حساب
  کاوه‌نگار بالا بیاورید و بعداً کلید را اضافه کنید.
- **شکست پیامک هرگز سفارش را خراب نمی‌کند.** ارسال با `after()` بعد از پاسخ
  به مشتری انجام می‌شود، پس مشتری پشت درخواست کاوه‌نگار معطل نمی‌ماند و اگر
  اعتبار حساب تمام شده باشد هم خرید سالم ثبت می‌شود.

**هزینه:** متن حدود ۸۵ نویسه است و چون پیامک فارسی هر ۷۰ نویسه یک بخش حساب
می‌شود، هر سفارش دو بخش هزینه دارد. عمداً کوتاه‌تر نشده چون حذف نام یا شماره
مشتری یعنی مدیر برای هر سفارش باید پنل را باز کند. اگر حجم سفارش‌ها بالا رفت،
متن را در `src/lib/sms/notify.ts` کوتاه کنید.

---

## ۶. اتصال به ترب

ترب پنل مدیریتی روی سایت ما ندارد؛ خودش دوره‌ای یک فید JSON را می‌خواند و
قیمت و موجودی را از آن به‌روز می‌کند.

**آدرس فید:**

```
https://novintajhiz.org/api/torob/products
```

این آدرس را از طریق پنل پشتیبانی ترب به آن‌ها بدهید. مسیر عمداً به `products`
ختم می‌شود، چون ترب همین را الزام کرده است.

**قالب خروجی** دقیقاً مطابق مستندات رسمی ترب است:

```json
{
  "count": "71",
  "max_pages": "1",
  "products": [
    {
      "title": "ال سی دی دستگاه اتوسیپپ لوون اشتاین تاچ",
      "subtitle": "Löwenstein",
      "page_unique": "1341",
      "current_price": "40000000",
      "availability": "instock",
      "category_name": "دستگاه‌های تنفسی",
      "image_link": "https://novintajhiz.org/images/products/p1341-1-1342.jpg",
      "page_url": "https://novintajhiz.org/products/ال-سی-دی-...",
      "short_desc": "...",
      "spec": { "برند": "Löwenstein", "کد کالا": "NT-1341" },
      "guarantee": "ضمانت اصالت کالا و پشتیبانی فنی"
    }
  ]
}
```

ترب سه پارامتر می‌فرستد (به‌صورت `form-data` با متد POST):

| پارامتر | کار |
|---|---|
| `page` | شماره صفحه؛ هر صفحه ۱۰۰ محصول |
| `page_unique` | شناسه یک محصول — خروجی همان یک محصول، ولی باز هم داخل آرایه |
| `page_url` | لینک یک محصول؛ پارامترهای `utm` انتهای لینک نادیده گرفته می‌شوند |

برای بررسی دستی، `GET` هم پاسخ می‌دهد:

```bash
curl "https://novintajhiz.org/api/torob/products?page=1"
```

### کدام محصولات به ترب می‌روند؟

فقط محصولاتی که **منتشر شده‌اند و قیمت دارند**. محصولات «تماس بگیرید» عمداً
کنار گذاشته می‌شوند — ترب موتور مقایسه قیمت است و رکورد بدون قیمت را
نمی‌پذیرد.

صفحه `/admin/torob` در پنل نشان می‌دهد چند محصول در فید هستند، کدام‌ها جا
مانده‌اند و چرا، و پیش‌نمایشی از خروجی واقعی. محصولات ناموجود حذف نمی‌شوند؛
با `availability: "outofstock"` فرستاده می‌شوند تا ترب رکوردشان را نگه دارد.

ترتیب محصولات بر اساس آخرین ویرایش است، همان‌طور که ترب می‌خواهد: هر محصولی
که قیمتش را عوض کنید در صفحه اول فید می‌آید و زودتر در ترب به‌روز می‌شود.

---

## ۷. امنیت و محدودیت‌ها

**هدرهای امنیتی** روی همه صفحه‌ها فعال‌اند: `X-Frame-Options`،
`X-Content-Type-Options`، `Referrer-Policy` و `Permissions-Policy`. مسیرهای
`/admin`، `/orders` و `/track` علاوه بر این `noindex` و `no-store` می‌گیرند.

> `Content-Security-Policy` عمداً گذاشته نشده. نکست برای هیدریشن از اسکریپت
> inline استفاده می‌کند و CSP سخت‌گیرانه بدون nonce سایت را می‌شکند. اگر
> روزی لازم شد، باید با nonce پیاده شود.

**محدودیت نرخ** روی مسیرهایی که بدون احراز هویت می‌نویسند:

| عملیات | سقف |
|---|---|
| آپلود فیش | ۱۰ در ۱۰ دقیقه |
| ثبت سفارش | ۲۰ در ۱۰ دقیقه |
| فرم تماس و دیدگاه | ۵ در ۱۰ دقیقه |
| خبرنامه | ۵ در ساعت |
| ورود به پیگیری سفارش | ۱۰ در ۱۰ دقیقه |

آخری مهم‌ترین است: بدون آن می‌شد با امتحان کردن کدهای پیگیری به سفارش دیگران
رسید.

> شمارنده در حافظه همان پروسه است. برای استقرار تک‌نمونه‌ای کافی است، ولی اگر
> برنامه را روی چند نمونه اجرا کردید هر نمونه سهم خودش را می‌شمارد و سقف
> واقعی چند برابر می‌شود. در آن حالت باید به Redis منتقل شود.

**پشت پراکسی:** محدودیت نرخ آدرس کاربر را از `X-Forwarded-For` می‌خواند. اگر
nginx این هدر را پاس ندهد، همه کاربران در یک سطل مشترک می‌افتند و زود بلوکه
می‌شوند. نمونه nginx بالا این هدر را ست می‌کند.

---

### ⚠ یک رفتار که باید بدانید: ISR در زمان قطعی دیتابیس

صفحه‌های عمومی ISR هستند و هر ساعت خودشان را بازتولید می‌کنند. خواندن‌ها
داخل `safeQuery` هستند، پس اگر دیتابیس قطع باشد سایت پایین نمی‌آید و
صفحه‌ها با محتوای خالی رندر می‌شوند — همان مقاومتی که عمداً طراحی شده.

ولی یک عارضه دارد: اگر دیتابیس **دقیقاً وقتی** قطع باشد که بازتولید
اجرا می‌شود، همان نسخه خالی در کش می‌نشیند و تا بازتولید موفق بعدی
(حداکثر یک ساعت) باقی می‌ماند.

نشانه‌اش این است که سایت بالاست ولی محصولات و مقالات نشان داده نمی‌شوند.
برای تشخیص، `/api/health/db` را چک کنید؛ اگر `database: down` بود مشکل از
دیتابیس است نه از اپ. بعد از برگشتن دیتابیس، یا یک ساعت صبر کنید یا
برنامه را در پنل لیارا ری‌استارت کنید تا کش پاک شود.

---

## ۸. معماری کش

سایت عمومی از دیتابیس می‌خواند، ولی در حالت عادی هیچ بازدیدی به دیتابیس
نمی‌رسد:

```
بازدیدکننده → کش صفحه نکست → unstable_cache (برچسب‌دار) → PostgreSQL
                                        ▲
                          ادمین ذخیره می‌کند → revalidateTag
```

هر کوئری عمومی در `src/lib/db/public.ts` با یک برچسب کش شده است. اکشن‌های پنل
بعد از هر نوشتن، `src/lib/db/tags.ts` را صدا می‌زنند و همان برچسب باطل می‌شود.
نتیجه: سرعت صفحه استاتیک، ولی به‌روزرسانی آنی بعد از ویرایش — بدون build دوباره.

`revalidate = 3600` روی صفحه‌ها فقط تور ایمنی است، برای حالتی که ایمیج بدون
دسترسی به دیتابیس ساخته شده باشد.

---

## ۹. به‌روزرسانی نسخه

```bash
git pull
docker compose up -d --build
```

مهاجرت‌های جدید خودکار اجرا می‌شوند. داده‌ها روی volume می‌مانند و دست نمی‌خورند.

برای بازگشت به نسخه قبل، ایمیج قبلی را اجرا کنید — ولی توجه کنید مهاجرت‌های
اجراشده برگشت‌پذیر نیستند مگر با `prisma migrate resolve`.
